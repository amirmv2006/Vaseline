<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>ir.amv.os.vaseline.data.spring</groupId>
        <artifactId>vaseline-data-spring-parent</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>vaseline-data-test-helper</artifactId>

    <dependencies>
        <dependency>
            <groupId>ir.amv.os.vaseline.data.spring</groupId>
            <artifactId>vaseline-dao-jpa-spring</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.data.spring</groupId>
            <artifactId>vaseline-data-advanced-search-jpa-spring</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-spring</artifactId>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>ir.amv.os.vaseline.data.apis</groupId>
                <artifactId>vaseline-advanced-search-maven-plugin</artifactId>
                <version>${project.version}</version>
                <executions>
                    <execution>
                        <id>generate-test-so</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate-so</goal>
                        </goals>
                        <configuration>
                            <srcFolder>src/main/java</srcFolder>
                            <srcEntitiesBasePackage>ir.amv.os.vaseline.data.test.model.server.entity</srcEntitiesBasePackage>
                            <destFolder>src/main/java</destFolder>
                            <destSearchObjectsBasePackage>ir.amv.os.vaseline.data.test.model.shared.searchobject</destSearchObjectsBasePackage>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>