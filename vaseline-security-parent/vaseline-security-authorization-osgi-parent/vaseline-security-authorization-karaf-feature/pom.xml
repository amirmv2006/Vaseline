<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>vaseline-parents-karaf-feature-parent</artifactId>
        <groupId>ir.amv.os.vaseline.parents.osgi</groupId>
        <version>2.0-SNAPSHOT</version>
        <relativePath>../../../vaseline-parents-parent/vaseline-parents-osgi-parent/vaseline-parents-karaf-feature-parent/pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
    <artifactId>vaseline-security-authorization-karaf-feature</artifactId>

    <dependencies>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authentication.osgi</groupId>
            <artifactId>vaseline-security-authentication-karaf-feature</artifactId>
            <version>${project.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.audit.osgi</groupId>
            <artifactId>vaseline-security-audit-karaf-feature</artifactId>
            <version>${project.version}</version>
            <classifier>features</classifier>
            <type>xml</type>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.basic.api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.basic.def</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.business.api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.business.def</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.model.api</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.model.def</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.dao.api</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.dao.def.hibernate</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.dao.def.jpa</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.business.api</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.apis</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.business.def</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.basic.osgi</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.basic.osgi.interceptor</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.dao.jpa.osgi</artifactId>
            <version>${project.version}</version>
        </dependency>
       <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.business.osgi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ir.amv.os.vaseline.security.authorization.osgi</groupId>
            <artifactId>ir.amv.os.vaseline.security.authorization.rbac.auto.user.annot.osgi</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.karaf.tooling</groupId>
                <artifactId>karaf-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>